import { Navbar, CarouselComp, Cards, Footer } from '../components';

export const Home = () => {
    return (
        <div className="home">
            <Navbar />
            <CarouselComp />
            <Cards />
        </div>
    );
};
