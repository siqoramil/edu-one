import React from 'react';
import { Navbar, Footer } from '../components';

export const Home = () => {
    return (
        <div className="home">
            <Navbar />
            <Footer />
        </div>
    );
};
